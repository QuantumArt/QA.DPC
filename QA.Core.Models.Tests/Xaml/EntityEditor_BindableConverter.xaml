<Group xmlns="http://artq.com/configuration"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            >
    <Group.Resources>
        <BindableConverter x:Key="conv"
                           Parameter="{Binding /Test1}"
                           ></BindableConverter>
        <!--ищет из корня-->
        <BindableConverter x:Key="rootconv"
                           Parameter="{Binding /Test, IsAbsolute=True}">
        </BindableConverter>
    </Group.Resources>
    <Label Title="{Binding /Test}" />
    <Label Title="{Binding /Test, Converter={Resource conv}}" />
    <EntityCollection Items="{Binding /TestItems}">
        <Label Title="{Binding ContentDisplayName, Converter={Resource rootconv}}"></Label>
    </EntityCollection>
</Group>

