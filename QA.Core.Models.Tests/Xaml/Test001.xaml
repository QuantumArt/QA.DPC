<QPControlTest xmlns="http://artq.com/configuration"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          Name="{x:Binding Test}"          
          Title="not binded">
    <QPControlTest Title="{Binding Path=Test1}" Name="{Binding AnotherCoolProperty}">
        <StackPanel>
            <QPControlTest Title="{Binding Path=Test1}" Name="{Binding AnotherCoolProperty}" />
            <QPControlTest Title="{Binding Inner.Prop}">static text</QPControlTest>
        </StackPanel>
    </QPControlTest>
</QPControlTest>

